> 2026-09-19 子代理独立评审（只读，依据当前源码行号）。结论：需先修改。design.md 已按 B1-B6 / S1-S7 修订，见各节「评审修订」。

# WorkspaceShell 状态 seam 计划评审\n\n## 结论\n\n**需先修改**：第一刀（Split）方向正确、可小改后开工；第二刀（SessionTabs）的核心前提与代码不符，必须重写 design §3 后才能动手。\n\n## 阻塞性问题\n\n**B1. design §3 的前提\"六个 active 指针靠 effect 互相同步\"不成立，指针实际由命令式多 setter 序列同步。**\n扫描全部 39 个 `useEffect` + 4 个 `useLayoutEffect`，触及 tab/active 状态的只有 11 个，其中没有一个是\"纯指针对齐\"：\n- L1133 / 1141 / 1145 / 1217：`xxxRef.current = xxx` 镜像（供异步回调读最新值，如 L942 `rdpSessionsRef`），删不掉，除非把 ref 一起消灭。\n- L1772-1799 / L1801-1834：`terminalFileLayoutByConnectionId`、`activeUnifiedTabByConnectionId` 的失效回退（含\"优先回退到 file tab\"的规则 L1824-1828），是带业务规则的派生，不是对齐。\n- L2474-2505 / L2507-2577：RDP/VNC 嵌入视口同步，真实副作用。\n- L1221-1258：实为 split 的 binding 失效清理（见 B2）。\n真正的指针同步在 46 个函数里手写，例如 `activateStandaloneTerminalTab` L5121-5135 一次写 `setActiveView`、`setActiveWorkspaceMode`、`setHomeActive`、`setActiveConnectionId`、`setActiveTabId` + `rememberActiveTab` + `rememberUnifiedActiveTab` + `syncCommandSenderTargetTab`；`returnHomeWhenWorkspaceEmpty` L4548-4554 一次写 7 个。所以\"删同步 effect 去掉一次渲染\"的收益不存在，而\"`tabs/activate` 必须原子地写 mode/home/connection/tab 四类字段\"的需求没有写进 action 清单。\n\n**B2. `activeWorkspaceMode`（19 处 setter）、`homeActive`（17 处）、`activeView` 完全不在 SessionTabs 状态形状里，但它们与六个指针在每个激活/关闭路径上同写。**\nL646 `type WorkspaceMode = \"home\"|\"ssh\"|\"local\"|\"rdp\"|\"vnc\"` 与 `WorkbenchTab.kind` 是同一枚举的两份表达；L1970 `showingHome = activeWorkspaceMode === \"home\" || (!hasSessionWorkspace && homeActive)` 说明 `homeActive` 是独立于 mode 的记忆位，不能简单派生。design §3 要么把两者纳入 `SessionTabsState`，要么说明为何留在外面且如何保证与 `activeTabId` 不双写。\n\n**B3. `activeTabByConnectionId`（L888）不是派生值，是\"每个连接上次激活的子 tab\"记忆，design §3 说\"改为 selector 派生\"会丢功能。**\n写入点 `rememberActiveTab` L4558、清除点 `forgetActiveConnectionTabs` L4588；`scripts/check-session-subtab-memory.mjs` 以源码断言该标识符存在。同类还有 `activeUnifiedTabByConnectionId` L1033 和 `commandSenderTargetTabByConnectionId` L984。它们应作为 `SessionTabsState.lastActiveByConnection` 一类的显式字段保留。\n\n**B4. Split 第一刀把 4 个\"派生状态归一 effect\"留在原地，会让 reducer 的不变量被 effect 二次改写。**\n- L1221-1258：tab 被关闭后剔除失效 binding，7 个 setter 连发。\n- L1459-1479：超过 `terminalSplitMaxPanes` 时重建四宫格。\n- L1481-1498：只剩 1 pane 时整体清空，并调用 `activateTerminalBindingAsStandalone`（跨 seam 写 SessionTabs）。\n- L1500-1528：sync 参与者收缩 + 少于 2 个时自动关同步。\n这四个 effect 的输入是 `terminalTabs`/`localTerminalTabs`（另一个 seam 的状态）+ split 自身状态。如果 reducer 只接收 `split/*` 意图动作，这些 effect 会变成\"读 reducer state → 再 dispatch 一串修正\"，等价于 reducer 之外还有第二个 reducer。应改为一个输入型动作 `split/availableBindingsChanged(availableKeys, connectedKeys)`，把归一逻辑放进 reducer；唯一的副作用（L1496）留在 effect 里，只在 reducer 返回 \"collapsed to standalone\" 结果时触发。design §2 目前只提到 `split/bindingsRemoved`，未覆盖 1459/1481/1500。\n\n**B5. design §2 表格三处标\"—\"的 setter 实际有调用点，且遗漏两个必须一起处理的 ref。**\n- `setTerminalSplitPickerOpenRequest` 5 处（L1490、4720、4780、4953、5088），`setTerminalSplitSyncParticipantKeys` 6 处（L1244、1493、1508、5015、5020、5091），`setTerminalSplitAutoCreateSameSession` 作为回调透传 L8962。\n- `terminalSplitPickerPendingPaneRef`（L1014）在 L1491、4779、4952、5089 与 `setTerminalSplitPickerOpenRequest` 成对清零，design 说\"两个 picker ref 不动\"会让 `split/clearPicker` 之后还要手写一行 ref 赋值，破坏\"一个动作一个变更\"。建议把 `pendingPaneId` 并入 `pickerRequest` 字段。\n- `terminalSplitIdRef`（L1023，`nextTerminalSplitId` L4611）是 pane/split id 计数器，reducer 需要新 id；要么由调用方生成并放进 payload，要么把计数器纳入 state。design 未提及。\n其余计数（layout 14 / host 6 / anchor 2 / tabActive 10 / focused 13 / revision 2 / syncEnabled 11 / syncError 8 / closeConfirm 1）与表格略有出入但量级一致，总计 78≈\"约 80\"，不阻塞。\n\n**B6. \"从 setter 序列翻译出的 action 序列\"不是 characterization。**\n它测的是新 reducer 是否符合作者对旧代码的理解，从头到尾不执行一行旧代码，重构前无法\"通过\"。可行方案见建议 S1。\n\n## 建议性改进\n\n**S1. 用\"先机械抽 hook、再换内脏\"做真正的 characterization。**\n第 0 步把 L1004-1023 的 12 个 useState + 3 个 ref + 上述 4 个 effect 原样搬进 `useTerminalSplitController(inputs)`（不改一行逻辑，只是移动），用 `@testing-library/react` `renderHook`（`// @vitest-environment jsdom`，依赖已在 package.json L62、L66）写 20 个左右的行为用例：开两 pane → 关一个 → 布局收缩且 host 保留、关最后一个 → 全清、sync 参与者收缩、revision 只在 resize end 自增（L9077）。这套用例先在旧实现上跑绿，再把 hook 内部换成 `useReducer`，用例不变仍绿。reducer 自身的纯函数测试在此之上追加。\n\n**S2. 多 setter 簇到 action 的映射需补齐，现有清单覆盖不到以下簇：**\n| 代码簇 | 现清单 | 缺什么 |\n|---|---|---|\n| L4704-4705 `activateTerminalSplitTab` | 无 | `split/activateTab`（tabActive=true, syncError=null） |\n| L5122-5123 / L6440-6441 | 无 | `split/deactivate`（tabActive=false, sync.enabled=false） |\n| L4763-4780 `assignTerminalSplitBinding` | 无 | `split/assignBinding`（layout+focus+清 picker） |\n| L4867-4891 四宫格 | 无 | `split/openFour` 或让 `split/open` 接受完整 layout |\n| L4930-4934 vs L5084-5092 | 都叫 `split/close` | pane 关到空时**不**清 picker/sync（靠 L1500 effect 下一帧补），group 关时全清；合并成一个动作会改变一帧内的中间态，需明确接受并记录 |\n| L5019-5031 | `sync.setParticipants` | 现实现在 updater 内部嵌套调 `setTerminalSplitSyncEnabled`（不纯），reducer 化是纯收益，但需要 action 同时表达\"participant 变化后 <2 则自动关闭\" |\n\n**S3. sync 建议一步到位放独立目录，不要\"先进 split 再搬\"。**\n`TerminalSplitLayout` props 只有 `syncEnabled`/`syncParticipantKeys`（TerminalSplitSurface.tsx L51-52），传入点 L9070-9071 只有两行；sync 的归一 effect（L1500）输入是 panes + tabs，本来就是跨 seam。放进 split 再搬会让 split 的 reducer.test 写两遍。第一刀直接建 `multiExec/` 只含 `{ mode, targets, error }`，第三刀再把 command sender 并入，触面反而更小；也避免 `split/sync.enable` 与后续 `multiExec/enable` 的命名重复。\n\n**S4. `WorkbenchTab` 合并的两个具体坑：**\n- `TerminalTab.index`（L4362、4383）不是排序位，是每连接内的编号，用于标题 \"终端 N\"（L12317-12327）。design 说\"`index` 退役\"会改标题；应改为\"保留为 `ordinal`，仅用于标题\"。\n- `terminalSplitAnchorIndex`（L5483、5546）是**按连接过滤后**子 tab 列表的插入位置。合并为全局 `tabs[]` 后该 index 语义变化，第二刀需同步改成\"按 owner tab id 锚定\"或在 selector 里重算。\n- 当前没有 tab 拖拽重排功能（全文无 reorder/moveTab 命中），所以 `tabs/move` 是预留，第二刀不要实现。\n- `remoteFileTabs` 在两处渲染（L8852 独立编辑器栈、L9084 unified 模式），加 `kind: \"editor\"` 不冲突，但 `UnifiedWorkbenchTab.kind` 的 `\"terminal\"|\"file\"` 与新 union 的 `\"ssh\"|\"editor\"` 是两套词，第二刀要给出映射函数，否则 L1920-1930 一段要改写。\n\n**S5. 明确列出会被改名影响的静态检查脚本。**\nCI 只跑 `check-startup-module-boundary-source.mjs`（ci.yml L74），但以下脚本以源码文本断言标识符，改 useState 为 reducer 后会失败：`check-command-sender-mvp-source.mjs`（`selectedCommandTargetKeys`、`commandSenderInput`、`commandSenderOpen`）、`check-session-subtab-memory.mjs`（`activeTabByConnectionId`）、`check-local-terminal-warmup-source.mjs`（`localTerminalTabs`）、`check-remote-file-editor-source.mjs`（`remoteFileTabs`）。implement.md 只提到前两个 command sender 脚本，且写在第三刀；`check-session-subtab-memory` 和 `check-local-terminal-warmup` 属第二刀，应写进第二刀验证项，并事先决定\"脚本改断言\"还是\"保留旧名作 selector 变量名\"。\n\n**S6. 顺序：保持 Split → SessionTabs → MultiExec，但第二刀不得在 B1-B3 修订前开工。** Split 先做是合理的排练，且 Task 05 快照同时需要 `SplitState`，不存在\"SessionTabs 先做更利于 Task 05\"的收益。\n\n**S7. 补充未言明的事项：**\n- dispatch 穿透：46 个写 tab 状态的函数都是组件内闭包，`dispatch` 引用稳定，无需穿线；但要规定\"action 携带意图（paneId/tabId），reducer 从 state 计算\"，不要像 L4928 那样在闭包里算好 `nextLayout` 再塞进 payload，否则保留了闭包过期读的隐患。\n- selector 稳定性：`sync.participantKeys` 是 `Set`，`terminalSplitPanes` 已 useMemo（L1442）；新 selector 若返回新对象要走 `useMemo`，否则 `TerminalSplitLayout` 与 xterm 面板多渲染。design §6 已提，但应写成规范条目进 state-management.md。\n- 基线数字修正：`useEffect(` 39 个（非 40），另有 4 个 `useLayoutEffect`；`useState` 108 是含类型引用的文本命中数，声明 78（组件内 77）。写进 implement.md 时区分。\n- `.trellis/spec/frontend/directory-structure.md` 与 `state-management.md` 目前都是空模板，`src/features/workspace/` 与现有 13 个 feature 目录无冲突，也无规范可违反；但需注意 `features/layout/` 已存在，`workspace` 与 `layout` 的职责边界应在 state-management.md 里一句话说清。\n\n## 已核实无误的点\n\n- `src/features/workspace/` 不存在；`terminalSplitLayout.ts` 纯函数与测试齐备，reducer 可直接复用不复制。\n- 五个集合（L903/905/907/996/1029）与六个指针（L886/887/888/912/913/1003，外加 `activeRemoteFileTabId` L1030 共七个）确实存在。\n- `terminalClearRequest`（L1021）与 `editorTerminalSplitPercent`（L1079）判定为\"不动\"正确。\n- Vitest 门禁与 `@testing-library/react`、`jsdom` 依赖就位，quality-guidelines 的\"reducer 优先、co-locate、显式 import、node 默认环境\"约定与本计划的目录/测试布局兼容。\n- 不加 feature flag、每刀独立提交可 revert、reducer 目录无外部消费者：成立。\n- 需求 §33 目标布局（Home/Server/WSL/RDP 单行 tab）与 `WorkbenchTab` union 方向一致；§16 仅要求 Telnet 支持 Split/MultiExec，对本任务无额外约束。"}],"container":null,"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,
